.claude/
node_modules/
dist/

# 機密ファイル (Supabase service_role キー等)
.env
.env.*
local-tools/.env
local-tools/.env.*
local-tools/asin-matches.json
local-tools/asin-review.csv

# local-tools generated artifacts (logs / scratch outputs)
local-tools/*.log
local-tools/paint-asin-results.json
local-tools/paint-review.txt
local-tools/*.out

local-tools/retry-safe.json
local-tools/*.out

# social 投稿のログ（PCローカルのみ）。※投稿キュー(social-queue.json)は
# GitHub Actions が投稿済み状態を管理するためリポジトリ管理する（ignoreしない）。
local-tools/social/*.log
